pluginManagement {
    repositories {
        mavenLocal()
        maven {
            name = 'comroid'
            url = 'https://maven.comroid.org'
        }
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'Herobrine'

include(':japi')
include(':ChatMod')
